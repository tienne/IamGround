import { SidenavItemDirective } from './sidenav-item.directive';

describe('SidenavItemDirective', () => {
  it('should create an instance', () => {
    const directive = new SidenavItemDirective();
    expect(directive).toBeTruthy();
  });
});
